on:
  push:
    branches:
      - main
    paths:
      - 'ansible/roles/bind/files/**'

jobs:
  run-ansible:
    runs-on: self-hosted
    steps:
      - name: run ansible playbook
        run: |
          cd ansible
          ansible-playbook dns.yml
